<?php
/**
 * telegram-config.php — секрети для відправки форм сайту в Telegram.
 * НЕ виводиться в браузер: цей файл лише повертає масив (return [...]),
 * нічого не echo'ить, тож навіть якщо хтось відкриє його URL напряму,
 * побачить порожню сторінку. Додатково закритий і в server/.htaccess.
 *
 * Що сюди вписати:
 *
 * 1. bot_token — від @BotFather у Telegram:
 *      напишіть @BotFather → /newbot → дайте ім'я боту → отримаєте токен
 *      виду "123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx".
 *      Якщо бот уже є — /mybots → ваш бот → API Token.
 *
 * 2. chat_id — куди бот надсилатиме повідомлення:
 *      - особистий чат: відкрийте чат з вашим ботом у Telegram, натисніть
 *        Start (або напишіть будь-що), потім у браузері відкрийте
 *        https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
 *        і знайдіть у відповіді "chat":{"id":ЦЕ_ЧИСЛО, ...} — це і є chat_id.
 *      - група/канал: додайте бота туди (для каналу — адміністратором),
 *        напишіть будь-яке повідомлення в групі/каналі і так само
 *        подивіться getUpdates; id групи/каналу буде від'ємним числом
 *        (напр. -1001234567890).
 *
 * Поки нижче стоять плейсхолдери — send-to-telegram.php відмовляється
 * відправляти і повертає зрозумілу помилку в консоль браузера, форми на
 * сайті при цьому продовжують чесно показувати повідомлення про помилку
 * замість фейкового "успіху".
 */
return [
    'bot_token' => 'ВСТАВТЕ_ТОКЕН_ВІД_BOTFATHER',
    'chat_id'   => 'ВСТАВТЕ_CHAT_ID',
];
