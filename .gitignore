# Секрети Telegram-форм — живуть тільки локально та на сервері, ніколи в git.
# У репозиторії лишається api/telegram-config.example.php з плейсхолдерами.
api/telegram-config.php

# Локальні налаштування інструментів розробника
.claude/

# JSON-ключі сервісного акаунта Google (GSC-вигрузка) — тільки в GitHub
# Secrets, ніколи в репозиторії. Шаблон покриває і перевипущені ключі.
panda-sushi-*.json

# Граф знань проєкту (graphify) — генерується локально, не деплоїться.
graphify-out/
